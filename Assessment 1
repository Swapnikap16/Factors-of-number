#include<stdio.h>
int main()
{
	int num, count = 1;
	printf("Enter a number:\n");
	scanf("%d", &num);

	printf("Factors are %d\n", num);
	
	while(count <= num)
	{
		if(num % count == 0)
		{
			printf("%d\n", count);
		}
		
		count++;
	}
}
